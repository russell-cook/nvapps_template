@{
    string trimmedModuleName = ViewBag.ModuleName.Replace(" ", "");
    string menuPath = string.Format("~/Views/{0}/_{1}Menu.cshtml", trimmedModuleName, trimmedModuleName);
}

<div class="row">
    <div class="col-md-2 col-xs-4">
        <br />
        <div class="panel panel-default">
            @{Html.RenderPartial(menuPath); }
        </div>
    </div>
    <div class="col-md-10 col-xs-8">
        <br />
